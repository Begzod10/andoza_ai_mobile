<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for API calls to the backend. Flutter only adds INTERNET to the
         debug/profile manifests, so release builds have no network access without
         this line (login fails with "Connection failed"). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- ARCore camera scanning (DepthScanActivity). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- AR is optional so the app still installs on non-AR devices/emulators. -->
    <uses-feature android:name="android.hardware.camera.ar" android:required="false" />
    <application
        android:label="Andoza AI"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Impeller has multiple documented crashes on Android emulators
             (flutter/flutter#105323, #175562, #148481, #163421) during
             route transitions/surface reconfiguration, matching a crash
             reproduced here (host-side segfault in the emulator's own
             RenderThread, right after login navigates while the IME hides).
             Falling back to Skia works around it; this only affects
             emulator rendering stability, not real devices. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
        <!-- ARCore is optional: the app runs (reporting "unsupported") without it. -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="optional" />
        <!-- Real ARCore room-scanning screen launched from LidarMethodChannel. -->
        <activity
            android:name=".DepthScanActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:screenOrientation="locked"
            android:theme="@style/LaunchTheme" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
